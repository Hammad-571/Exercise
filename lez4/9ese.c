#include <stdio.h>
#include <ctype.h>
int main() {
#define LEN 100
    int len;
    do{
        printf("Enter the length of array");
	scanf("%d", &len);
    }while(i < 1);
    double num[i];
    double total = 0.00;
    for(int i = 0; i < len;i++)
    {
	printf("Enter the number ");
	scanf("%lf", &num[i]);
	total = total + num[i];
	
    }
    printf("the average is %lf", (total/len));
    printf("the varianza %lf",)
    
    }

